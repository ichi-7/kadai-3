<h2>Books</h2>
<table>
  <th></th>
  <th>Title</th>
  <th>Opinion</th>
  <% @books.each do |book| %>
  <tr>
    <td><%= image_tag 'no_image.jpg' %></td>
    <td><%= link_to book.title, book_path(book) %></td>
    <td><%= book.body %></td>
  </tr>
  <% end %>
</table>